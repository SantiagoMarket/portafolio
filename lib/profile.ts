/**
 * El título público vive aquí. Hero, pestaña, tarjeta y mail lo importan:
 * si se vuelve a escribir a mano, el test de superficies lo caza.
 */
export const roleTitle = "Systems Integrator";

/** Líneas del H1. El acento (borgoña) va en la última. */
export const roleTitleLines = ["SYSTEMS", "INTEGRATOR"] as const;

export const documentTitle = `Santiago Cubillos — ${roleTitle}`;

export const shellPrompt = "santiago@systems:~";

export const roleKeywords = [
  roleTitle,
  "Automation",
  "CRM",
  "HubSpot",
  "n8n",
  "Make",
  "Bogotá",
];
